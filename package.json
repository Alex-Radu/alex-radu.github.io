{
  "name": "the-stairway",
  "version": "0.0.1",
  "description": "Learning project",
  "main": "index.js",
  "scripts": {
    "build": "rm -f ./dist/*.html && rm -f ./index.html && node ./build/builder.js && prettier --write ./dist/*.html && prettier --write ./index.html"
  },
  "author": "Alex-Radu",
  "license": "MIT",
  "dependencies": {
    "dot": "^1.1.3",
    "showdown": "^1.9.1"
  },
  "devDependencies": {
    "prettier": "1.19.1"
  }
}
